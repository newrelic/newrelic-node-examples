# `@as-integrations/koa@1.1.1` peer-pins koa@^2.0.0 even though its runtime
# code only uses ctx (compatible with koa 3). Allow npm to install across the
# stale peer constraint until upstream publishes a koa-3-aware version.
legacy-peer-deps=true
