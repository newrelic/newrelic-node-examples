{
  "name": "new-relic-langchain-aws-example",
  "version": "0.0.0",
  "dependencies": {
    "@langchain/aws": "^1.0.0",
    "@langchain/core": "^1.0.0",
    "fastify": "^4.25.2",
    "newrelic": "^13.6.0"
  },
  "resolutions": {
    "@langchain/core": "^1.0.0"
  },
  "overrides": {
    "@langchain/core": "^1.0.0"
  },
  "pnpm": {
    "overrides": {
      "@langchain/core": "^1.0.0"
    }
  },
  "scripts": {
    "start": "node --env-file .env -r newrelic index.js",
    "start:debug": "node --inspect-brk --env-file .env -r newrelic index.js",
    "start:no-agent": "node --env-file .env index.js",
    "start:debug:no-agent": "node --inspect-brk --env-file .env index.js"
  }
}