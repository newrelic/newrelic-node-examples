{
  "name": "profiling-example-app",
  "version": "1.0.0",
  "description": "Exercises the New Relic Node agent's CPU and heap profilers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node -r newrelic --inspect --env-file .env dist/index.js",
    "start:source-map": "npm run build && NEW_RELIC_PROFILING_SOURCE_MAPPING_ENABLED=true node -r newrelic --inspect --env-file .env dist/index.js",
    "load": "npm run build && node dist/load.js"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "express": "^4.21.2",
    "newrelic": "^14.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3"
  }
}
